id: example
description: |
  Attrs built with `inner_html(...)` carry raw DOM content.
taint:
  sources:
    - shape: |
        @html.Attrs::build().inner_html($_)
  sinks:
    - shape: |
        @html.div(class=$_, attrs=__SOURCE__, $_)
  sanitizers:
    - shape: sanitize(__SOURCE__)
