id: ellipsis-inside
description: |
  Reuse the surrounding argument prefix inside the target.
patterns:
  - shape: wrapper($$$args, __TARGET__)
    then:
      patterns:
        - shape: target($$$args)
