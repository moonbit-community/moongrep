// YAML schema validation lives in load.mbt. Semantic validation that depends on
// parsed shapes is implemented alongside compilation in compile.mbt for now.
// This file remains as the package-local home for future validation-only
// helpers.
