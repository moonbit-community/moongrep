rule(
  name: "embed",
  command: "moon runwasm moonbit-community/embed $input -o $output",
)

dev_build(rule: "embed", input: "WritingRules.md", output: "writing_rules.mbt")

dev_build(rule: "embed", input: "RuleSpec.md", output: "rule_spec.mbt")
