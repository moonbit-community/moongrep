///|
pub let docs : ReadOnlyArray[(String, String, String)] = [
  (
    "RuleSpec", "YAML rule keys, validation, and matcher semantics.", rulespec_md,
  ),
  (
    "CLISpec", "Command-line parsing, scanning, output, diagnostics, and exit behavior.",
    clispec_md,
  ),
]
