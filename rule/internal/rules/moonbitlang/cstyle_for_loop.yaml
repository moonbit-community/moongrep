id: cstyle_for_loop
description: |

inside-expr:
  shape: |
    for $(counter:id) = $(start:exp); $(counter:id) < $(limit:exp); $(counter:id) = $(counter:id) + 1 {
      __TARGET__
    }
patterns:
  - shape: counter