id: inspect_number
description: |
  Found inspect() snapshots whose expected value is a plain number.
  Prefer numeric assertions for numeric checks.

patterns:
  - shape: |
      inspect(__, content=$(str:const))
    guard:
      $str: "^-?(0|[1-9][0-9]*)(\\.[0-9]+)?$"
