id: cstyle_for_loop
description: |

inside-expr: |
  for $(counter:id) = $(start:exp); $(counter:id) < $(limit:exp); $(counter:id) = $(counter:id) + 1 {
    __TARGET__
  }
patterns:
  - shape: $(counter:id)
